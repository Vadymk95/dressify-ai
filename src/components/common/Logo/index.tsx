import { FC } from 'react';

export const Logo: FC = () => {
    return (
        <div className="flex items-center gap-2">
            <span className="text-md md:text-xl font-bold">DressifyAI</span>
        </div>
    );
};
