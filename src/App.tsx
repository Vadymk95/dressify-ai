import { Footer, Header, Main } from '@/components/layout';
import { FC } from 'react';

export const App: FC = () => {
    return (
        <>
            <Header />
            <Main />
            <Footer />
        </>
    );
};
