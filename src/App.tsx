import { FC } from 'react';
import { Footer, Header, Main } from '@root/components/layout';

export const App: FC = () => {
    return (
        <>
            <Header />
            <Main />
            <Footer />
        </>
    );
};
